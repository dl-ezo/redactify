services:
  app:
    build: .
    container_name: redactify
    env_file:
      - path: .env
        required: false
    environment:
      - PORT=5001
    ports:
      - "5001:5001"
    restart: unless-stopped
